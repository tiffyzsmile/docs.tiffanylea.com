---
name: Docker
menu: Tools
route: /tools/docker
---

import ExternalLink from "../../components/ExternalLink";

# Docker

## Overview

Docker CLI: <ExternalLink href="https://docs.docker.com/engine/reference/commandline/cli/" text="Docker CLI" />

Docker Compose CLI: <ExternalLink href="https://docs.docker.com/compose/reference/" text="Docker Compose CLI" />

## Common CLI

```sh
// list all containers
$ docker ps -all

// stop container in list
$ docker stop [CONTAINERID]

// remove unused data
$ docker system prune

// remove unused local volumes
$ docker volume prune
```
